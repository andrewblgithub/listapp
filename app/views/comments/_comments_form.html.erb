<div class="panel-body row">
    <%= form_for [@list, @post, @post.comments.build], :remote => true do |f| %>
        <div class="field col-sm-10">
            <%= f.text_field :content, class: "form-control", placeholder: "Add Comment" %>
        </div>
        <div class="col-sm-2">
            <%= f.submit "Submit", class:"btn btn-submit btn-primary form-control" %>
        </div>
    <% end %>
</div>
